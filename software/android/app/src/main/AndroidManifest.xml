<?xml version="1.0" encoding="utf-8"?>
<!-- Tucklet Android manifest. License: PolyForm Noncommercial 1.0.0 -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- BLE control plane -->
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />

    <!-- Wi-Fi data plane: programmatic join to the charm's SoftAP -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Companion association keeps the device bonded + allows background BLE -->
    <uses-feature android:name="android.hardware.bluetooth_le" android:required="true" />

    <!-- Photos via MediaStore (scoped). On 33+ use READ_MEDIA_*; legacy below. -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <!-- Background trickle as a (data-sync) foreground service when actively moving bytes -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:label="Tucklet"
        android:icon="@android:drawable/star_on"
        android:theme="@style/Theme.Tucklet"
        android:supportsRtl="true">

        <activity
            android:name=".ui.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- WorkManager is initialized by its default provider; the trickle work
             is enqueued from the app. No custom configuration required. -->
    </application>
</manifest>
